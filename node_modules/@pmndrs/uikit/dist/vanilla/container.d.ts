import { ContainerProperties, createContainer } from '../components/container.js';
import { AllOptionalProperties } from '../properties/default.js';
import { Parent } from './utils.js';
export declare class Container<T = {}> extends Parent<T> {
    private mergedProperties?;
    private readonly styleSignal;
    private readonly propertiesSignal;
    private readonly defaultPropertiesSignal;
    private readonly parentContextSignal;
    private readonly unsubscribe;
    internals: ReturnType<typeof createContainer>;
    constructor(properties?: ContainerProperties, defaultProperties?: AllOptionalProperties);
    getComputedProperty<K extends keyof ContainerProperties>(key: K): ContainerProperties[K] | undefined;
    getStyle(): undefined | Readonly<ContainerProperties>;
    setStyle(style: ContainerProperties | undefined, replace?: boolean): void;
    setProperties(properties: ContainerProperties | undefined): void;
    setDefaultProperties(properties: AllOptionalProperties): void;
    destroy(): void;
}
